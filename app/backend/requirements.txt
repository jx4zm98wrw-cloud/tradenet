# Runtime dependencies — pinned for reproducible builds.
# Regenerate via `pip-compile requirements.in --output-file=requirements.txt`
# (requirements.in tracks bounds; requirements.txt tracks exact versions).

# === Library deps (preserved from monolithic extractor) ===
pdfplumber==0.11.10        # was 0.11.4, bump pulls patched pdfminer.six (CVE-2025-64512 + 70559)
pandas==3.0.3
numpy==2.4.6
colorama==0.4.6
tqdm==4.68.3

# === Image extractor (app/backend/image_extractor/, lazy-imported by worker) ===
pymupdf==1.27.2.3
pillow==12.2.0
psutil==7.2.2
pyyaml==6.0.3

# === API + DB ===
fastapi==0.136.3              # was 0.115.5
starlette==1.3.1              # explicit pin. fastapi requires >=0.46.0 (no upper cap);
                              # the real cap was prometheus-fastapi-instrumentator<1.0,
                              # lifted by bumping it to 8.0.0 below. 1.3.1 clears the
                              # 0.49.x advisories (CVE-2026-48817/48818/54282/54283,
                              # PYSEC-2026-161).
uvicorn[standard]==0.49.0
sqlalchemy[asyncio]==2.0.51
alembic==1.18.4
asyncpg==0.31.0
psycopg2-binary==2.9.12
python-multipart==0.0.32      # clears CVE-2026-53538/53539/53540 (fixed in 0.0.30/0.0.31)
                              # and the earlier CVE-2026-24486/40347/42561 advisories.
                              # which isn't on PyPI yet — ignored in pip-audit per ci.yml
pydantic==2.13.4
pydantic-settings==2.14.2

# === Auth / security ===
python-jose[cryptography]==3.5.0  # was 3.3.0, clears PYSEC-2024-232 + 233 + 2025-185
passlib[bcrypt]==1.7.4
slowapi==0.1.10

# === Worker ===
redis==8.0.0
rq==2.10.0
requests==2.34.2             # WIPO Madrid Monitor HTTP client (madrid_enrich + sweep job).
                             # Was an undeclared runtime dep (present in the host venv but
                             # missing from the image, so the containerized worker failed to
                             # import worker.madrid_sweep).

# === Observability ===
structlog==26.1.0
sentry-sdk[fastapi]==2.63.0
prometheus-fastapi-instrumentator==8.0.0   # 8.x supports starlette>=1.0 (7.x capped <1.0)

# === File handling ===
python-magic==0.4.27

# === Similarity engine (api.similarity) ===
# Phonetic + string similarity: Jaro-Winkler + Metaphone via jellyfish — the
# trademark industry's standard pair (Cohen-Ravikumar-Fienberg 2003). Pure C,
# no extra runtime deps. Perceptual visual hashing for logo comparison via
# imagehash on the existing Pillow install — extracted PNGs already live in
# the data dir, so pHash compares the actual specimens rather than wordmark
# text alone.
jellyfish==1.2.1
imagehash==4.3.2
