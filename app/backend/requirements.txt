# Runtime dependencies — pinned for reproducible builds.
# Regenerate via `pip-compile requirements.in --output-file=requirements.txt`
# (requirements.in tracks bounds; requirements.txt tracks exact versions).

# === Library deps (preserved from monolithic extractor) ===
pdfplumber==0.11.9         # was 0.11.4, bump pulls patched pdfminer.six (CVE-2025-64512 + 70559)
pandas==2.2.3
numpy==2.1.3
colorama==0.4.6
tqdm==4.67.1

# === Image extractor (app/backend/image_extractor/, lazy-imported by worker) ===
pymupdf==1.27.2.3
pillow==12.2.0
psutil==7.2.2
pyyaml==6.0.3

# === API + DB ===
fastapi==0.118.3              # was 0.115.5, pulls starlette 0.49 (PYSEC-2026-161 + CVE-2025-54121 + 62727)
uvicorn[standard]==0.32.1
sqlalchemy[asyncio]==2.0.36
alembic==1.14.0
asyncpg==0.30.0
psycopg2-binary==2.9.10
python-multipart==0.0.20      # was 0.0.17; latest available. Future CVE-2026-* require >=0.0.22
                              # which isn't on PyPI yet — ignored in pip-audit per ci.yml
pydantic==2.10.3
pydantic-settings==2.7.0

# === Auth / security ===
python-jose[cryptography]==3.5.0  # was 3.3.0, clears PYSEC-2024-232 + 233 + 2025-185
passlib[bcrypt]==1.7.4
slowapi==0.1.9

# === Worker ===
redis==5.2.0
rq==1.16.2

# === Observability ===
structlog==24.4.0
sentry-sdk[fastapi]==2.19.0
prometheus-fastapi-instrumentator==7.0.0

# === File handling ===
python-magic==0.4.27
