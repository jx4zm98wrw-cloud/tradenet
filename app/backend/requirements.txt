# Runtime dependencies — pinned for reproducible builds.
# Regenerate via `pip-compile requirements.in --output-file=requirements.txt`
# (requirements.in tracks bounds; requirements.txt tracks exact versions).

# === Library deps (preserved from monolithic extractor) ===
pdfplumber==0.11.4
pandas==2.2.3
numpy==2.1.3
colorama==0.4.6
tqdm==4.67.1

# === API + DB ===
fastapi==0.115.5
uvicorn[standard]==0.32.1
sqlalchemy[asyncio]==2.0.36
alembic==1.14.0
asyncpg==0.30.0
psycopg2-binary==2.9.10
python-multipart==0.0.17
pydantic==2.10.3
pydantic-settings==2.7.0

# === Auth / security ===
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
slowapi==0.1.9

# === Worker ===
redis==5.2.0
rq==1.16.2

# === Observability ===
structlog==24.4.0
sentry-sdk[fastapi]==2.19.0
prometheus-fastapi-instrumentator==7.0.0

# === File handling ===
python-magic==0.4.27
