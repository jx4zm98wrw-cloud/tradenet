# Dev-only dependencies — lint, test, type-check.
-r requirements.txt

ruff==0.8.2
mypy==1.13.0
types-PyYAML==6.0.12.20260518   # type stubs for the lazy `import yaml` in worker.ingest
pytest==8.3.4
pytest-asyncio==1.4.0
httpx==0.28.0
